JS ToolBox
==========

Interested in HTML5 changes, I've created this repo to try out some features,
like rendering to canvas elements.

JS Clock
--------

This is a simple, analog clock that runs on the local time of the machine that loads it. It
can be configures to be of any size, trying to keep proportions.
